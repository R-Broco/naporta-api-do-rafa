name: SRE Traffic Generator

on:
  schedule:
    # Dispara a cada 5 minutos no GitHub Actions
    - cron: '*/5 * * * *'
  workflow_dispatch: # Permite rodar manualmente com 1 clique no GitHub

jobs:
  run-simulation:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Executar Simulação
        env:
          API_URL: 'https://naporta-api-do-rafa.onrender.com'
        run: node simulate-traffic.js
